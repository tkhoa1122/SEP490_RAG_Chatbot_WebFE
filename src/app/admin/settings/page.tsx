import { AdminSettingsClient } from "@/components/dashboard/AdminSettingsClient";

export const metadata = {
  title: "Cài đặt | Smart Shopping Admin",
};

export default function AdminSettingsPage() {
  return <AdminSettingsClient />;
}
